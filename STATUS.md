<!-- STATUS v1.0 -->

## Meta
<!-- SECTION:meta -->
- project: cc-panel
- session: 7
- updated: 2026-04-18 02:15
- plan: PLAN.md
- repo: https://github.com/tftdatascientist/cc-panel (public, main)
<!-- /SECTION:meta -->

## Done
<!-- SECTION:done -->
- Phase 0 scaffold — package.json (komenda ccPanel.open, pusta configuration ccPanel), tsconfig.json (ES2022/CJS/strict), esbuild.config.js, src/extension.ts (stub WebviewPanel "Hello cc-panel" w ViewColumn.One z reveal-existing), .vscodeignore, .gitignore, .vscode/{launch,tasks}.json dla F5. `npm install` OK (6 pkg), `npm run build` produkuje out/extension.js (2.9 KB), `tsc --noEmit` przechodzi. **Potwierdzone ręcznie przez użytkownika (F5 działa).**
- Phase 1 — `node-pty@^1.0.0` w deps (prebuilds win32-x64 obecne), `esbuild.config.js` external `node-pty`, `.vscodeignore` dopuszcza `node_modules/node-pty/**`. `src/terminals/TerminalManager.ts` — klasa `TerminalManager` tworząca `vscode.Terminal` przez `Pseudoterminal` wrapping node-pty (`cmd.exe /c cc` na win32), env `CC_PANEL_TERMINAL_ID=1`, cwd=workspaceFolder|homedir, xterm-256color, obsługa handleInput/setDimensions/close, onExit → auto-cleanup. `src/extension.ts` — po otwarciu webview w ViewColumn.One wykonuje `workbench.action.newGroupBelow` i tworzy terminal w ViewColumn.Two; `ensureTerminal()` idempotentne; `deactivate()` wywołuje `terminalManager.dispose()`. Bundle 6.6 KB, `tsc --noEmit` czysto. **Potwierdzone ręcznie przez użytkownika (F5 split + CC #1 działa).**
- Phase 2 — `resources/webview/{index.html,styles.css,main.js}` z layoutem 20/60/20 (4 status tiles / buttons|messages / 4 info bars), ramka w 4 kolorach per terminal (T1=teal, T2=amber, T3=purple, T4=coral), CSS grid z `color-mix` dla bg/border. `src/panel/messages.ts` — typed union `PanelOutboundMessage` + `PanelInboundMessage`, type guard `isTerminalId`. `src/panel/PanelManager.ts` — klasa `PanelManager implements Disposable`, tworzy `WebviewPanel` z `localResourceRoots: [extensionUri/resources]` i `enableScripts: true`, renderuje HTML z podstawieniem `{{CSP_SOURCE}}/{{NONCE}}/{{STYLES_URI}}/{{MAIN_URI}}` (CSP z nonce dla script-src), routing `onDidReceiveMessage` do callbacków, API `setTerminals()/setActive()/reveal()`. `src/extension.ts` przepisany na PanelManager + TerminalManager; split wykonuje się tylko przy pierwszym otwarciu. Bundle 8.8 KB, `tsc --noEmit` czysto. **Potwierdzone ręcznie (F5 layout i event bus działają).**
- Phase 3 — `resources/hooks/statusline.js` (Node script: stdin JSON → `~/.claude/cc-panel/state.{id}.json` + echo krótki status line na stdout, wypisuje model i cost). `chokidar ^3.6.0` w deps, external w esbuild, `fsevents` też external. `.vscodeignore` whitelistuje chokidar + tranzytywne deps. `src/state/StateWatcher.ts` — `chokidar.watch(stateDir/state.*.json)` z `awaitWriteFinish`, emituje `TerminalState` przez `EventEmitter`. `src/panel/messages.ts` rozszerzone o `setMetrics` + `TerminalMetrics`. `PanelManager.setMetrics()` dodany, webview `main.js` obsługuje `setMetrics` (aktualizuje `[data-field]` w infobar dla model/ctx/cost/mode). `src/hooks/installHooks.ts` — komenda `ccPanel.installHooks` wpisuje hooki do `~/.claude/settings.json` z obsługą backupu przy kolizji. `src/extension.ts` — uruchamia `stateWatcher.start()` w activate, `onChange` → `panelManager.setMetrics()` jeśli `terminal_id` ∈ {1..4}.
- Phase 4 kod — `resources/hooks/userpromptsubmit.js` i `stop.js` — czytają stdin (ignorują), read-modify-write `state.{id}.json` ustawiając `phase` (working / waiting) i `phase_changed_at` (ISO). `installHooks.ts` rozszerzony o `upsertHook()` wstawiający wpisy do `settings.hooks.UserPromptSubmit` i `settings.hooks.Stop` (deduplikacja po nazwie skryptu — re-install nie duplikuje). `TerminalState` rozszerzony o `phase` + `phase_changed_at`. `messages.ts` — `setPhase` wymienił `timerSeconds` na `sinceMs` (epoch). `PanelManager.setPhase()` dodany. `main.js` webview — `setPhase(id, phase, sinceMs)` startuje `setInterval` 1s tickujący `tile-timer` z formatu `Ns` / `NmMMs`; per-tile Map timerów, czyszczenie przy zmianie fazy. `extension.ts` → `onStateChange` wywołuje `panelManager.setPhase()` gdy `state.phase` obecny.
- Phase 5 kod — `resources/default-buttons.json` (8 slash-command buttons: /clear, /compact, /status, /model, /cost, /help, /resume, /review). `package.json` — configuration `ccPanel.buttons` (array z walidacją schema label+type+value+icon). `src/buttons/ButtonStore.ts` — czyta config, fallback do `default-buttons.json`, waliduje, `onDidChangeConfiguration` → refresh. `src/buttons/Actions.ts` — `execute(button, activeTerminalId)` wywołuje `terminals.write(id, value + '\r')` dla `sendText` (dla `keystroke` bez `\r`). `TerminalManager.write(id, data)` dodany — pobiera `ptyProcess` przez `getPty()` i wywołuje `.write()`. `messages.ts` — `setButtons` outbound + `invokeButton` inbound, `ButtonViewSpec { label, icon? }` (bez `value` — ekstensja trzyma prawdziwy spec). `PanelManager.setButtons()` + routing `invokeButton` → callback. Webview `main.js` — `setButtons(buttons)` renderuje `<button class="action-btn">` do `#button-grid`, klik → `postMessage({type:"invokeButton", index})`. CSS `.action-btn` z `color-mix` hover/active. `extension.ts` — `activeTerminalId` tracked (aktualizowany przy `onSelectTerminal`), `onInvokeButton` → `Actions.execute()`, przy braku pty pokazuje warning toast. Bundle 18.3 KB, `tsc --noEmit` czysto.
- Publikacja 2026-04-17 — `git init` w `VS_CLAUDE/` (osobny repo od nadrzędnego CEM), `main` branch, initial commit `ffdd9f3`, `gh repo create cc-panel --public --source=. --push` → https://github.com/tftdatascientist/cc-panel (publisher `tftdatascientist`). README.md po polsku dodane: opis, architektura, wymagania, uruchomienie dev, komendy, konfiguracja przycisków, tabela faz. Przeskanowane pod kątem sekretów — brak. `.gitignore` pokrywa `node_modules/`, `out/`, `*.vsix`, `.vscode-test/`.
- Phase 6 kod — skalowanie 1→4 terminali + messages feed z CC Stop. `messages.ts` — `MessageItem { terminalId, text, at }`, outbound `setMessages`/`addMessage`, inbound `addTerminal`. `TerminalManager` — `activeIds()`, `onTerminalsChanged` EventEmitter (fire po `create` i `onDidCloseTerminal`). `PanelManager` — `messages: MessageItem[]` (rolling 100), `addMessage()` buforuje + postuje, `onAddTerminal` callback, `broadcastInit` dohydryca `setMessages` dla ponownie otwartego webview. `StateWatcher` — pola `last_message`, `last_message_at` w `TerminalState`. `resources/hooks/stop.js` — parsuje JSON ze stdin, czyta ostatnie 64 KB `transcript_path`, wyciąga ostatnią wiadomość `assistant` (text blocks joined, trim 500 chars), zapisuje `last_message` + `last_message_at`. `extension.ts` — komenda `ccPanel.addTerminal` (znajduje najniższy wolny id 2/3/4), handler `onAddTerminal` z webview spawnuje terminal i ustawia active, `onTerminalsChanged` → `panelManager.setTerminals()` + reshift active jeśli aktualny zniknął, `lastMessageSeen: Map<id, at>` dedupuje pierwszy hydrating read vs. nowe Stop evenity → `panelManager.addMessage()`. Webview `main.js` — `enabledTerminals` Set, setTerminals ustawia `+ add` dla disabled tiles ale pozostawia je klikalne, klik na disabled → `addTerminal`, `appendMessage`/`setMessages` renderują pozycje z timestampami w `messages-feed`, pasek koloru per terminal. `styles.css` — `.msg.msg-tN` z `border-left` w kolorach T1-T4, disabled tile ma kursor pointer i hover. `package.json` — komenda `ccPanel.addTerminal`. Bundle 20.9 KB, `tsc --noEmit` czysto.
- Phase 7 kod — `ccPanel.cycleActive` + keybinding contribution. `package.json` — komenda `ccPanel.cycleActive` w contributes.commands; `contributes.keybindings` z `ctrl+alt+tab` (mac: `cmd+alt+tab`), bez `when` clause — binding globalny (w terminalu konflikt z completion obchodzimy przez modifier Alt). `src/extension.ts` — `cycleActiveTerminal()` pobiera `terminalManager.activeIds()` (już posortowane asc), filtruje przez `isTerminalId`, szuka `indexOf(activeTerminalId)` i bierze `(idx+1) % len`; jeśli idx==-1 bierze pierwszy; aktualizuje `activeTerminalId`, wywołuje `terminal.show(false)` (bez kradzenia fokusu z CC) oraz `panelManager.setActive(next)` dla ramki webview. Obsługa edge case: 0 terminali → no-op; 1 terminal → no-op. Handler komendy zarejestrowany przez `vscode.commands.registerCommand`. Bundle 21.5 KB, `tsc --noEmit` czysto.
- Phase 8 kod — akcja typu `keystroke` w button grid. Silnik (`src/buttons/Actions.ts`) już istniał od Phase 5 — `terminals.write(id, value)` bez trailing `\r`, odróżnione od `sendText`. `resources/default-buttons.json` rozszerzone z 8 → 11 buttonów: `Esc` (`\u001b`), `Ctrl+C` (`\u0003`), `Shift+Tab` (`\u001b[Z` — CSI Z reverse tab, używane przez CC do cyklu trybów normal→auto-accept→plan). JSON escape sequences weryfikowane runtime: Esc→0x1B, Ctrl+C→0x03, Shift+Tab→0x1B 0x5B 0x5A. Walidacja w `ButtonStore.validate()` przyjmuje `type: "keystroke"` z Phase 5 (enum w package.json + runtime check). Bundle 21.5 KB bez zmian (JSON to resource, nie bundlowane), `tsc --noEmit` czysto.
- Phase 9 kod — edytor przycisków. `ButtonStore.save(buttons): Promise<void>` — `config.update("buttons", serialized, ConfigurationTarget.Global)` (zgodne ze spec "settings sync across devices"); po zapisie `onDidChangeConfiguration` sam triggeruje reload i `onChange` emitter → webview refresh, żadnego ręcznego re-push do panelu. `src/buttons/EditButton.ts` — funkcja `runEditButton(store)` z natywnymi VS Code dialogs (QuickPick + InputBox, bez custom webview modal). Flow: QuickPick root `[...istniejące, + Dodaj nowy, ✕ Usuń]`, akcja `edit|new` → wizard `collectSpec()` (label → typ sendText/keystroke → wartość z walidacją niepustą → icon opcjonalny); akcja `delete` → multi-pick QuickPick + filter. Dla keystroke wartość konwertowana przez `encodeForDisplay` (niedrukowalne < 0x20 i 0x7f → `\uXXXX`) i `decodeEscapes` (`\uXXXX`, `\xXX`, `\n`, `\r`, `\t`) — user widzi `\u001b` zamiast niewidzialnego ESC. `package.json` — komenda `ccPanel.editButton`. `src/extension.ts` — rejestracja komendy wywołuje `runEditButton(buttonStore)`. Bundle 26.9 KB (+5.4 KB vs Phase 8), `tsc --noEmit` czysto.
- Post-MVP iter 1 (ctx-warn + vsCodeCommand + per-workspace target) —
  (1) **Red tile ctx>=70%** — `resources/hooks/statusline.js` kalkuluje `ctx_pct` (suma `input_tokens` + `cache_read_input_tokens` + `cache_creation_input_tokens` podzielona przez 200k, clamp do 100); zapisuje w `state.ctx_pct` i wypluwa do stdout jako `ctx:NN%`. `StateWatcher.TerminalState.ctx_pct?: number`. `messages.TerminalMetrics.ctxPct?: number`. `extension.onStateChange` przekazuje zarówno `ctx` (string z `%`) jak i `ctxPct` (raw number) do `setMetrics`. `main.js` `setMetrics` toggluje klasę `is-ctx-warn` na tile gdy `ctxPct >= 70`. `styles.css` `.tile.is-ctx-warn` — border-color `var(--phase-red)` + bg color-mix 12% red. Warunek ortogonalny do phase — ostrzeżenie o pamięci widoczne jednocześnie z working/waiting.
  (2) **vsCodeCommand akcja** — `ButtonActionType += "vsCodeCommand"`. `Actions.execute` case `vsCodeCommand` → `void vscode.commands.executeCommand(value)`, zwraca true (fire-and-forget). `ButtonStore.validate` whitelist + `package.json` enum rozszerzone. `EditButton.typeItems` ma 3. opcję; `titleForValue`/`promptForValue` mapują na "VS Code command ID" (np. `workbench.action.files.save`). `previewValue` symbol: sendText=➤, keystroke=⌨, vsCodeCommand=⚙.
  (3) **Per-workspace overrides** — `ButtonStore.save(buttons, target?: ConfigurationTarget = Global)`. `EditButton.pickTarget()` — jeśli są workspaceFolders, pokazuje QuickPick "Globalnie (user settings, Settings Sync)" vs "Workspace (tylko ten projekt, nadpisuje global)"; jeśli brak workspace — defaultowo Global bez pytania. `describeTarget` dla komunikatu po zapisie. Wywoływany zarówno w edit/new flow jak i w delete flow przed save.
  Bundle 29.1 KB (+2.2 KB vs Phase 9), `tsc --noEmit` czysto.
- Post-MVP iter 2 (promptTemplate) — `src/buttons/Placeholders.ts`: regex `/\{input:([^}]+)\}/g`, `extractPlaceholderLabels()` zbiera unikalne (pierwsze wystąpienie, trim), `resolvePlaceholders(value, buttonLabel)` async iteruje po etykietach, każdą pyta raz przez `showInputBox` z title `${buttonLabel} — ${label} (i/n)` i `ignoreFocusOut: true`, Esc w którymkolwiek pytaniu → zwraca `undefined` (anulacja całej akcji), inaczej jeden `replace(regex)` zamieniający każde wystąpienie tej samej etykiety tym samym wynikiem. `Actions.execute` async, zwraca `ExecuteResult = "ok" | "noTerminal" | "cancelled"`; dla `sendText` woła `resolvePlaceholders` i przerywa cicho gdy `cancelled`. `extension.ts` `onInvokeButton` zawinięty w `void (async () => {...})()` z `await actions.execute`; warning toast tylko dla `noTerminal`, `cancelled` = silent. Placeholdery aktywne tylko dla `sendText` (keystroke to raw bajty, vsCodeCommand to stały ID). `EditButton.collectSpec` prompt dla sendText wspomina o `{input:Label}`. Bundle 30.6 KB (+1.5 KB vs iter 1), `tsc --noEmit` czysto.
- Session 6 (2026-04-18) — hardening po pierwszym F5 end-to-end u użytkownika. Cztery fixy:
  (1) **installHooks: 3-opcyjny dialog zamiast Podmień/Anuluj** — `src/hooks/installHooks.ts` przy kolizji `statusLine` pokazuje modal z "Podmień (backup)" / "Zachowaj mój" / Cancel. "Zachowaj mój" przeskakuje zapis `settings.statusLine` ale dalej instaluje UserPromptSubmit + Stop → fazy working/waiting i messages feed działają nawet gdy user trzyma własny statusLine (np. `npx ccstatusline@latest`). Notyfikacja końcowa zawiera dopisek `(statusLine: zachowany Twój — metryki model/ctx/cost w kafelkach pozostaną puste)`.
  (2) **Terminale T2/T3/T4 jako split T1, nie nowe grupy edytora** — `TerminalManager.create(id, location)` akceptuje unię `vscode.ViewColumn | { parentTerminal: vscode.Terminal }`; dla `parentTerminal` używa `TerminalSplitLocationOptions` (VS Code renderuje split w tej samej grupie). `src/extension.ts` `addTerminal(id)` dla id≠1 wybiera `{ parentTerminal: T1 }` jeśli T1 istnieje; fallback do `ViewColumn.Two` tylko gdy T1 zniknął. Wcześniej każdy kolejny terminal otwierał się w osobnej kolumnie/grupie edytora.
  (3) **Kolorowe ikony tabów per terminal** — `package.json` `contributes.colors` deklaruje `ccPanel.terminal.t1..t4` (domyślnie #14b8a6/#f59e0b/#a78bfa/#fb7185 dla dark/light/highContrast). `TerminalManager.create` ustawia `color: new vscode.ThemeColor("ccPanel.terminal.t" + id)` w `createTerminal` — tab w VS Code terminal sidebar/splitcie ma kolor zgodny z ramką webview.
  (4) **Hooki filtrują obce sesje CC** — `resources/hooks/{statusline,userpromptsubmit,stop}.js` na wejściu sprawdzają `CC_PANEL_TERMINAL_ID` regexem `/^[1-4]$/` i robią `process.exit(0)` gdy zmiennej brak lub ma inną wartość. Cel: hooki w `~/.claude/settings.json` są globalne — odpalą się dla KAŻDEJ sesji CC (w tym głównej użytkownika). Bez filtra pisały `state.0.json` zaśmiecając stan. StateWatcher i tak filtrował przez `isTerminalId` (1-4), ale state.0.json potrafił skonfudować debug i był niepotrzebną operacją IO przy każdym Stop w dowolnej sesji CC.
  Bundle 31.7 KB (+1.1 KB vs iter 2), `tsc --noEmit` czysto.
- Session 7 (2026-04-18) — domknięcie post-MVP iter 3 + iter 4 + fix race init.
  (1) **Post-MVP iter 3 multiStep** — `ButtonSpec.value: string | ButtonStep[]` z nowym union type `ButtonActionType | "multiStep"`. `ButtonStep { type, value }` bez label/section/icon (kroki dziedziczą z parenta). `ButtonStore.validate` z pomocniczą `validateSteps()` odfiltrowującą wpisy bez `{type ∈ {sendText, keystroke, vsCodeCommand}, value: string}` — pusta tablica po walidacji odrzuca cały button. `Actions.execute` dla `multiStep` iteruje po value array, każdy krok woła `executeStep(type, value, stepLabel)` z `stepLabel = "${button.label} — krok i/n"` (placeholder prompts pokazują który krok trwa); pierwszy `cancelled`/`noTerminal` przerywa łańcuch. `executeStep` wyekstrahowany z dawnego switch — sendText await `resolvePlaceholders`, keystroke raw write, vsCodeCommand fire-and-forget. `EditButton.collectSpec` dla `existing.type === "multiStep"` pokazuje infobox "edycja kroków tylko w settings.json" i pozwala zmienić tylko label/section/icon, `value` zostaje bez zmian; `previewValue` dla multiStep wypisuje `⛓ N kroków: step1 → step2 → ...`. `package.json` schema `ccPanel.buttons.value` rozszerzone o `oneOf: [string | array of {type, value}]`; enum type dostaje `multiStep`.
  (2) **Post-MVP iter 4 sekcje** — `ButtonSpec.section?: string` opcjonalne. `ButtonViewSpec.section?` dodane w `messages.ts`. `extension.ts` mapuje `{ label, icon, section }` zamiast `{ label, icon }` przy pushu do webview (dwa miejsca: `ccPanel.open` + `buttonStore.onChange`). Webview `setButtons` parsuje sekcje: trzyma `currentSection` zmienną, dla każdego buttona porównuje `b.section.trim()` — jeśli różne od poprzedniego, renderuje `.section-header` (grid-column 1/-1 z border-top) przed buttonem; pierwsza sekcja dostaje klasę `section-header-first` bez border-top. Buttony bez section po sekcji z nazwą → `.section-separator` (cienka linia). `styles.css` — `.section-header` font-size 10px uppercase letter-spaced muted z border-top; `.section-separator` 1px background border. `EditButton.collectSection()` pomocniczy InputBox (opcjonalny, puste = bez sekcji) wywoływany między value i icon w wizardzie. `default-buttons.json` przepisany z sekcjami: "Slash" (/clear, /compact, /status, /model, /cost, /help, /resume, /review), "Keys" (Esc, Ctrl+C, Shift+Tab), "Combo" (Reset ctx multiStep /compact → /clear).
  (3) **broadcastInit zawsze dosyła setButtons** — `PanelManager.broadcastInit` wywołuje `post({type:"setButtons", buttons: this.buttons})` niezależnie od `buttons.length`. Cel: pierwszy `setButtons` przed `ready` event z webview gubił się w race; teraz `webview.ready` → `broadcastInit` → pewne setButtons, nawet gdy puste array (webview pokaże "Brak przycisków" hint).
  Bundle 35.0 KB (+3.3 KB vs session 6), `tsc --noEmit` czysto.
<!-- /SECTION:done -->

## Current
<!-- SECTION:current -->
- task: session 7 — domknięcie wszystkich zaplanowanych iteracji post-MVP. Iter 3 (multiStep), iter 4 (sekcje), fix race `broadcastInit setButtons`. Zmiany zbudowane + typ-checknięte; weryfikacja ręczna F5 przez użytkownika pozostaje (tak samo jak z sesji 4-6).
- files: session 7 zmodyfikowała `src/buttons/ButtonStore.ts` (union `type: ButtonActionType | "multiStep"`, `value: string | ButtonStep[]`, helper `validateSteps`, pole `section` w spec), `src/buttons/Actions.ts` (executeStep wyekstrahowany, multiStep iteruje po krokach z etykietami `"${label} — krok i/n"` dla placeholderów), `src/buttons/EditButton.ts` (`collectSection` pomocniczy, `multiStep` edytowalny tylko przez label/section/icon, `previewValue` wypisuje łańcuch kroków), `src/panel/messages.ts` (`ButtonViewSpec.section?`), `src/panel/PanelManager.ts` (broadcastInit zawsze dosyła setButtons), `src/extension.ts` (mapowanie `{label, icon, section}`), `resources/webview/main.js` (`setButtons` renderuje `.section-header`/`.section-separator` przy zmianach section), `resources/webview/styles.css` (`.section-header`, `.section-header-first`, `.section-separator`), `resources/default-buttons.json` (sekcje Slash/Keys/Combo + przykład multiStep "Reset ctx"), `package.json` (schema buttons.value oneOf string|array, enum type += multiStep, pole section). Nowych plików brak.
- state: bundle 35.0 KB, `tsc --noEmit` czysto. Repo main @ commit `ffdd9f3` (initial) — sesje 4+5+6+7 niezacommitowane. Pozostało commit+push jako ostatni krok sesji.
- blocker: brak blokerów kodowych. Weryfikacja ręczna w Extension Dev Host wymaga reload po commicie (Ctrl+R w host) i kliknięcia `Reset ctx` aby zobaczyć sekwencyjne execute + pokazanie sekcji "Slash"/"Keys"/"Combo" jako nagłówki w button-grid.
- next_step: (A) Commit+push wszystkich zmian sesji 4-7 na `origin/main`. (B) Nowy cykl: post-MVP iter 5 — persistence messages feed (bufor rolling 100 na dysku w `~/.claude/cc-panel/messages.json` + rehydrate on activate, by nie gubić historii po reload window). (C) Opcjonalne: VSIX packaging + publishing, dokumentacja usage README dla multiStep/section.
<!-- /SECTION:current -->

## Next
<!-- SECTION:next -->
- [x] Phase 1 — jeden terminal CC spawnowany przez node-pty widoczny w editor grupie pod panelem
- [x] Phase 2 — pełny layout webview (trzy rzędy) + event bus ekstensja↔webview
- [~] Phase 3 — statusline hook + chokidar state watcher → dolne paski info (kod done, czeka na weryfikację F5)
- [~] Phase 4 — hooki UserPromptSubmit/Stop → górne kafelki working/waiting z timerem (kod done; red/ctx>70% dodane w Post-MVP iter 1)
- [~] Phase 5 — button grid z sendText z default-buttons.json + ccPanel.buttons config (kod done)
- [~] Phase 6 — skalowanie 1-4 terminali (klik w disabled tile/komenda addTerminal) + ramka aktywnego w 4 kolorach + messages feed agregujący Stop events z transcript_path (kod done, czeka na weryfikację)
- [~] Phase 7 — komenda ccPanel.cycleActive + keybinding contribution (default Ctrl+Alt+Tab) (kod done, czeka na weryfikację)
- [~] Phase 8 — akcja typu keystroke (Esc, Ctrl+C, Shift+Tab dla cyklu trybów CC) (kod done, czeka na weryfikację)
- [~] Phase 9 — edytor przycisków (modal: label, typ, wartość, ikona opcjonalna) + persistence w configuration (kod done, czeka na weryfikację)
- [~] Post-MVP iter 1 — red tile ctx>=70% + vsCodeCommand + per-workspace overrides (kod done, czeka na weryfikację)
- [~] Post-MVP iter 2 — promptTemplate z placeholderami `{input:Label}` (kod done, czeka na weryfikację)
- [~] Post-MVP iter 3 — multiStep (value jako array akcji, sekwencyjne await) (kod done, czeka na weryfikację)
- [~] Post-MVP iter 4 — grupowanie/sekcje przycisków (header w spec + CSS separator) (kod done, czeka na weryfikację)
- [ ] Post-MVP iter 5 — messages feed persistence (rolling 100 na dysk + rehydrate on activate)
<!-- /SECTION:next -->
